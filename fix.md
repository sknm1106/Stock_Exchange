## 수정 요구사항 정리

### 1. 포인트 컬러 변경

서비스 전체의 메인 포인트 컬러를 건국대학교 녹색으로 통일합니다.

```text
#00703E
```

적용 대상:

* 주요 버튼
* 선택된 메뉴
* 제목 및 아이콘
* 입력창 포커스 테두리
* 카드 테두리 및 hover 효과
* 로그인 화면 강조 요소
* 관리자 권한 활성화 표시
* 주요 구분선

단, 주가 상승·하락을 나타내는 색상은 의미 구분을 위해 기존 색상을 유지합니다.

```text
상승·수익: 초록색
하락·손실: 빨간색
주의: 주황색
일반 정보: 파란색
```

---

### 2. 다크 모드를 브라이트 모드로 변경

현재 어두운 배경의 다크 테마를 밝은 테마로 변경합니다.

주요 디자인 기준:

```text
전체 배경: 흰색 또는 밝은 회색
카드 배경: 흰색
기본 글자: 진한 회색 또는 검은색
보조 글자: 중간 회색
테두리: 연한 회색
포인트 컬러: #00703E
```

예시 색상:

```text
페이지 배경: #F7F9F8
카드 배경: #FFFFFF
기본 글자: #1F2937
보조 글자: #6B7280
기본 테두리: #E5E7EB
포인트 컬러: #00703E
```

기존 다크 모드용 흰색 글자, 반투명 검은 배경, 짙은 남색 배경은 모두 브라이트 모드에 맞게 수정합니다.

---

### 3. 헤더 구성 변경

#### 기존 학사모 이모티콘 제거

헤더의 학사모 이모티콘을 삭제합니다.

```text
🎓 삭제
```

대신 다음 학교 로고 이미지를 표시합니다.

```text
assets/konkuk_logo.png
```

#### 헤더 제목 변경

기존 서비스 제목을 다음 문구로 변경합니다.

```text
공과대학 전공박람회 주식 시장
```

#### 헤더 설명 문구 변경

기존:

```text
건국대학교 공과대학 12개 학과 모의 주식 거래소
```

변경:

```text
건국대학교 공과대학 학과 모의 주식 거래소
```

즉, 설명 문구에서 `12개`를 제거합니다.

#### 최종 헤더 구성

```text
[건국대학교 로고]

공과대학 전공박람회 주식 시장
건국대학교 공과대학 학과 모의 주식 거래소
```

---

### 4. 날짜와 시간을 한국시간으로 변경

현재 `datetime.now()`를 그대로 사용하고 있다면 서버 위치에 따라 UTC 또는 다른 시간으로 표시될 수 있으므로 한국 표준시로 변경합니다.

한국시간 기준:

```text
Asia/Seoul
UTC+9
```

Python 적용 예시:

```python
from datetime import datetime
from zoneinfo import ZoneInfo

korea_now = datetime.now(ZoneInfo("Asia/Seoul"))
```

기존 코드의 다음 부분을 찾아 변경합니다.

```python
datetime.now()
```

변경:

```python
datetime.now(ZoneInfo("Asia/Seoul"))
```

적용 대상:

* 로그인 시간
* 거래 체결 시간
* 주문 시간
* 뉴스 작성 시간
* 관리자 가격 변경 시간
* 사용자 접속 시간
* 화면에 표시되는 현재 시간

---

### 5. 관리자 페이지 접근 방식 변경

#### 왼쪽 사이드바에서 관리자 페이지 숨기기

현재 사이드바에 표시되는 `Admin` 또는 `관리자` 메뉴를 일반 사용자에게 보이지 않도록 숨깁니다.

일반 사용자는 다음 메뉴만 확인할 수 있게 구성합니다.

```text
홈
로그인
주식 시장
포트폴리오
뉴스
```

관리자 페이지는 일반적인 페이지 목록이나 왼쪽 탭에서 직접 노출하지 않습니다.

#### 관리자 로그인 조건

로그인 페이지에서 다음 정보를 입력하면 관리자 계정으로 인식합니다.

```text
학번: admin777
이름: admin777
```

두 값이 모두 정확하게 일치해야 합니다.

```python
student_id == "admin777" and name == "admin777"
```

관리자 로그인 성공 시:

* 세션에 관리자 권한 저장
* 관리자 페이지 이동 버튼 또는 관리자 메뉴 표시
* 관리자 전용 기능 사용 가능

예시 세션 값:

```python
st.session_state["is_admin"] = True
```

일반 학생 로그인 시:

```python
st.session_state["is_admin"] = False
```

관리자 페이지에서도 직접 주소 접근을 방지하기 위해 관리자 여부를 다시 확인해야 합니다.

```python
if not st.session_state.get("is_admin", False):
    st.error("관리자만 접근할 수 있는 페이지입니다.")
    st.stop()
```

---

### 6. 로그인 화면 안내 카드 내용 변경

기존 로그인 페이지 안내 내용:

```text
📱 QR 간편 접속
별도 가입 절차 없음

🪙 100 Coin 지급
최초 로그인 시 자동 선물

⏰ 1시간 가격 변동
학과별 시세 변동
```

위 내용을 상품 안내 영역으로 변경합니다.

#### 변경할 제목

```text
상품 안내
```

#### 상품 내용

```text
🥇 1등 상품
에어팟

🥈 2등 상품
스마트 워치

🥉 3등 상품
스타벅스 2만원 상품권
```

최종 표시 예시:

```text
🎁 상품 안내

🥇 1등 상품
에어팟

🥈 2등 상품
스마트 워치

🥉 3등 상품
스타벅스 2만원 상품권
```

카드형 UI로 구성한다면 다음처럼 배치하는 것이 좋습니다.

```text
[1등 상품]
에어팟

[2등 상품]
스마트 워치

[3등 상품]
스타벅스 2만원 상품권
```

